const screenWidth = 600;
const screenHeight = 600;
const frameR = 20;
const cellDimension = 20; // grid cell dimension

module.exports = {
    screenWidth,
    screenHeight,
    frameR,
    cellDimension,
}